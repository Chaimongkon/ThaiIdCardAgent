# Notice

ThaiIdCardAgent uses Windows PC/SC APIs and references the PCSC NuGet package version 7.0.1. Thai ID card data reading is intentionally not implemented until a verified protocol provider is configured and tested.